| File                                                   | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
|--------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| [BankAccount.kt](BankAccount.kt)                       | Create a bank account system applying **Encapsulation** to secure the balance and its operations. <br/>Step by step: <br/>1. Create `BankAccount` class (val `holder`, val `numberAccount`). <br/>2. Define `balance: Double` as **private** (initial value 0.0). <br/>3. Create a public getter method `getBalance()` to read the private balance. <br/>4. Implement `deposit(amount: Double)`: must only allow amounts greater than zero. <br/>5. Implement `withdraw(amount: Double): Boolean`: must check for positive amount AND sufficient funds (`balance >= amount`) before proceeding.                                                                                                                   |
| [InventoryManagement.kt](InventoryManagement.kt)       | Design an inventory system applying **Encapsulation** to protect stock integrity. <br/>Business Rule: Stock must never be set directly or fall below zero. <br/>Step by step: <br/>1. Create `ProductStock` with immutable properties (`val name`, `val code`). <br/>2. Define `stockQuantity: Int` as **private** (initial value 0). <br/>3. Create public method `recordStockIn(quantity: Int)` to safely increase stock (must accept only positive values). <br/>4. Create public method `recordStockOut(quantity: Int)` that verifies the removal quantity and returns `Boolean` for success/failure. <br/>5. Create `getQuantity()` to safely read the private stock value.                                  |
| [AuthService.kt](AuthService.kt)                       | Build an authentication service applying **Encapsulation** to manage a sensitive Access Token. <br/>Step by step: <br/>1. Create the `AuthService` class. <br/>2. Define `currentToken: String?` as **private** (initial value null). <br/>3. Create `login(user: String, generatedToken: String)` to change the private token and call a private logging method. <br/>4. Create `logout()` to reset the private token to null. <br/>5. Create `isLoggedIn(): Boolean` to publicly check if the private token is present.                                                                                                                                                                                         |
| [DataProcessors.kt](DataProcessors.kt)                 | Design a data analysis system applying **Inheritance** and **Polymorphism** for file processing. <br/>Step by step: <br/>1. Create an `abstract class DataProcessor` with a `val fileName: String`. <br/>2. Define a concrete, inheritable method `validateFile()` (returns Boolean). <br/>3. Define an `abstract method processData()` (returns String). <br/>4. Create child classes `JsonProcessor` and `XmlProcessor` that inherit from `DataProcessor`. <br/>5. **Override** `processData()` in each child to return a unique processing message specific to JSON or XML, demonstrating polymorphism.                                                                                                        |
| [PaymentGateways.kt](PaymentGateways.kt)               | Integrate payment methods applying **Inheritance** and **Polymorphism** for fee calculation. <br/>Step by step: <br/>1. Create an `open class PaymentGateway` with `val name: String`. <br/>2. Define a concrete, inheritable method `connect()`. <br/>3. Define an `open fun calculateFee(amount: Double)` (default implementation returns 0% fee). <br/>4. Create child classes `CreditCardGateway` and `PayPalGateway`. <br/>5. **Override** `calculateFee()` in the child classes: `CreditCardGateway` (3.5% fee: `amount * 0.035`) and `PayPalGateway` (4.9% fee: `amount * 0.049`), showcasing polymorphism.                                                                                                |
| [EmployeeSystem.kt](EmployeeSystem.kt)                 | Create an employee system applying **Inheritance** (Abstract Class) and **Interfaces** for role differentiation. <br/>Step by step: <br/>1. Create the `abstract class Employee` (name, baseSalary) with an `abstract fun calculateBonus()`. <br/>2. Define an `interface Authenticated` with `fun authenticate(password: String): Boolean`. <br/>3. Create `Developer` (inherits `Employee`, 10% bonus). <br/>4. Create `Manager` (inherits `Employee`, implements `Authenticated`, 15% bonus). <br/>5. The main function must use **Polymorphism** (storing as `Employee`) and **Type Checking** (`if (employee is Authenticated)`) to handle role-specific operations.                                         |
| [InteractiveBookstore.kt](InteractiveBookstore.kt)     | Create a system to manage bookstore items (Books, Magazines, Bookmarks) using **Inheritance** and **Polymorphism** to apply unique pricing rules. <br/>Step by step: <br/>1. Create an **`abstract class BookstoreItem`** (val `title`, val `basePrice`). <br/>2. Define a concrete method `displaysDetails()` and an `abstract fun calculateFinalPrice(): Double`. <br/>3. Create child classes: `Book` (final price = `basePrice + 5.00`), `Magazine` (final price = `basePrice - 2.00`), and `Bookmark` (final price = `basePrice`). <br/>4. The main function must use a polymorphic list (`List<BookstoreItem>`) to store instances and iterate through them, calling `calculateFinalPrice()` for each item. |
| [VehicleFleetManagement.kt](VehicleFleetManagement.kt) | Create a vehicle fleet management system applying **Composition** ("Vehicle HAS-A Engine") and **Inheritance** (Abstract Class) for maintenance cost calculation. <br/>Step by step: <br/>1. Define the **`Engine`** class (power, status, ligar/desligar methods). <br/>2. Define the **`abstract class Vehicle`** (make, year) that includes a **`val engine: Engine`** in its constructor (Composition). <br/>3. Define an `abstract fun maintenanceCost()`. <br/>4. Create child classes **`Car`** (fixed R$ 300.00 cost) and **`Truck`** (R$ 300.00 + `enginePower * 0.5`) that override `maintenanceCost()` (Polymorphism).                                                                                 |
| [BankingTransactions.kt](BankingTransactions.kt)       | Create an account system using **Composition** (Transaction History) and **Interfaces** (Security Log) for risk management. <br/>Step by step: <br/>1. Define a `data class Transaction` (value, type). <br/>2. Define an `interface Loggable` with `fun logRegister(): String`. <br/>3. Create the `Account` class (holder, balance) with a **private** `MutableList<Transaction>` (Composition). <br/>4. Implement `deposit()` and `withdraw()`. <br/>5. For `withdraw()`, if the amount $>= 1000.00$, create an object that implements `Loggable` (e.g., `HighValueWithdraw`) to generate an extra security log entry.                                                                                         |